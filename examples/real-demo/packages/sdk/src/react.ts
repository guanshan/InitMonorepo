/**
 * TanStack Query hooks bundle. Re-exports the Orval-generated React hooks
 * so the web app can call typed `useQuery` / `useMutation` helpers without
 * touching the fetcher directly.
 */
export * from "./generated/hooks";
