VITE_API_BASE_URL=
VITE_APP_NAME=Real Demo
VITE_DEFAULT_LOCALE=en
VITE_DEFAULT_THEME=system
# Leave empty to use the React Router dev server proxy and same-origin API paths.
# Set an absolute URL only when intentionally targeting a cross-origin API.
# Set during the web build for non-root deployments.
# VITE_BASE_PATH=/real-demo
